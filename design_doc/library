We require a way to maintain a library of recipes. To that end we break down 
each recipe into tasks. Tasks are then entries in the database.

The attributes to store for each entry are:

The relationships to store for each entry are:
dependencies: 


Two databases:

Tasks:
Tasks -> Attributes, Dependencies database entry

Dependencies:
Dependency arrays for each task, link to other tasks