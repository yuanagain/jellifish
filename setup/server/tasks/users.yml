# Rushy Panchal
# tasks/users.yml
# Contains the user creation directives for Ansible

- name: create user
  action: user name={{ config.user.name }} append=yes groups={{ ",".join(config.user.groups) }} state=present
  